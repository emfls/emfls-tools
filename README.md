# emfls-tools

`tools.emfls.com`에서 제공하는 무료 생활·업무 웹도구 사이트입니다.

## 개발

- Astro + TypeScript + Vanilla CSS
- 정적 출력: `dist/`
- 개발: `npm run dev`
- 검사: `npm run check`
- 빌드: `npm run build`

Cloudflare Pages는 `main` 브랜치에서 `npm run build`를 실행하고 `dist`를 배포합니다.
